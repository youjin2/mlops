name: predict_pawpularity_score
entry_points:
  baseline:
    command: "python -m src.baseline"
  keras_naive:
    command: "python -m src.keras_naive"
  keras_conv2d:
    command: "python -m src.keras_conv2d"
